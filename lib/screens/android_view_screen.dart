import 'package:flutter/material.dart';

class AndroidViewScreen extends StatelessWidget {
  const AndroidViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Android View')),
    );
  }
}
