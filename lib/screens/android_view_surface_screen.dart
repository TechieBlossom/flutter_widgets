import 'package:flutter/material.dart';

class AndroidViewSurfaceScreen extends StatelessWidget {
  const AndroidViewSurfaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Android View Surface')),
    );
  }
}
